/** @format */

import {AppRegistry} from 'react-native';
import PrimaryNav from './App/Navigation/AppNavigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PrimaryNav);
